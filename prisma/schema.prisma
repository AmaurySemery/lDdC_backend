generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Localisation {
  id             String        @id @default(uuid()) @db.Uuid
  world          String
  coordinates    Json
  description    String
  places         Json
  name           String
  characters     Character[]
  chronologies   Chronology[]
}

model Character {
  id             String        @id @default(uuid()) @db.Uuid
  background     String
  picture        String
  race           String
  localisation   Localisation  @relation(fields: [localisationId], references: [id])
  localisationId String        @db.Uuid
}

model Chronology {
  id             String        @id @default(uuid()) @db.Uuid
  events         Json
  plot           Boolean
  localisation   Localisation  @relation(fields: [localisationId], references: [id])
  localisationId String        @db.Uuid
}

model User {
  id             String        @id @default(uuid()) @db.Uuid
  name           String        @unique
  role           String
  password       String
}